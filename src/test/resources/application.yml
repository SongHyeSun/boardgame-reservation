# 테스트 전용: PostgreSQL 없이 H2 인메모리로 실행 (PostgreSQL 호환 모드)
spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

app:
  admin:
    email:
    password:
